export type SearchBook = {
  author: string;
  title: string;
};

export type Book = {
  book_image: string;
  author: string;
  title: string;
};

export type Category = {
  display_name: string;
  list_name_encoded: string;
};
